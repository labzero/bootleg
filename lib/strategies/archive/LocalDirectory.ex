defmodule Bootleg.Strategies.Archive.LocalDirectory do
  @moduledoc ""

  def init(%Bootleg.Config = config) do

  end

  def archive(%Bootleg.Config = config, %Bootleg.Build = build) do
    
  end
end